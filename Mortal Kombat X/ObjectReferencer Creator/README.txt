CPP = Source
EXE = Compiled

Place CombinedReferencer.ObjectReferencer inside "Input".
Run the tool.

You will then need to inject the file somehow into the package,
either by MKX Package Appendor or by MKX Offset Fixer.

